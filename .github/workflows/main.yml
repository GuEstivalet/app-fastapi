name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout app repository
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            guestivalet/app-fastapi:latest
            guestivalet/app-fastapi:${{ github.sha }}

      - name: Checkout manifests repository
        uses: actions/checkout@v3
        with:
          repository: 'GuEstivalet/manifests-fastapi'
          ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
          path: manifests-fastap

      - name: Update Kubernetes manifest
        run: |
          cd manifests-fastapi/k8s
          sed -i 's|image: .*|image: guestivalet/app-fastapi:${{ github.sha }}|' deployment.yaml

      - name: Commit and push changes
        run: |
          cd manifests-fastap
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add deployment.yaml
          git commit -m "Update image tag to ${{ github.sha }}"
          git push

